name: Deploy to Cloudflare Workers (disabled duplicate)

# Disabled — use deploy.yml only to avoid race conditions
on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This duplicate workflow is disabled. Use deploy.yml."
